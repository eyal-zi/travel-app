import { Calendar } from '../Calendar/Calendar'
import { HomePageRoot } from './HomePage.styles'

export const HomePage = () => {
  return (
    <HomePageRoot>
      <Calendar />
    </HomePageRoot>
  )
}
