// Display label + MUI palette colour for each workflow status. Shared by the
// request card, its list, and the response dialog. Backed by the common
// request-status meta shared with file requests.
export { REQUEST_STATUS_META as STATUS_META } from '../../../../common/requests/requestStatus'
